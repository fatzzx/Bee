#ifndef SPECIAL_CARDS_HPP
#define SPECIAL_CARDS_HPP
#include "game.hpp"

#include <stdio.h>

void plus_four(){
  // change_color();
  // for(int i = 0; i < 4; i++) pit_card(&main, current->next);
  // for(int i = 0; i < 2; i++) {current = current->next; current = current->next};
}

void plus_two(){
  // for(int i = 0; i < 2; i++) pit_card(&main, current->next);
  // for(int i = 0; i < 2; i++) {current = current->next; current = current->next};
}

void block(){
  // for(int i = 0; i < 2; i++) {current = current->next; current = current->next};
}

void reverse(){
  
}

#endif